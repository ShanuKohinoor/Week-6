<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="flex justify-center items-center min-h-screen bg-gradient-to-br from-[#f0f4f8] via-[#e0f7f4] to-[#cfeef7] m-0 p-0">

  <div class="max-w-md w-full bg-white rounded-2xl shadow-lg p-8 text-gray-800">
    <!-- Profile Header -->
    <div class="flex flex-col items-center mb-6">
      <div class="bg-[#0a9396] text-white w-24 h-24 flex items-center justify-center rounded-full text-4xl font-bold shadow-md">
        <%= student.name.charAt(0).toUpperCase() %>
      </div>
      <h1 class="text-2xl font-semibold mt-4"><%= student.name %> Profile</h1>
    </div>

    <!-- Profile Details -->
    <div class="space-y-3">
     <div class="flex justify-between border-b border-gray-200 pb-2">
        <span class="font-medium text-gray-600">Name:</span>
        <span class="text-gray-800"><%= student.name %></span>
      </div>

      <div class="flex justify-between border-b border-gray-200 pb-2">
        <span class="font-medium text-gray-600">Email:</span>
        <span class="text-gray-800"><%= student.email %></span>
      </div>
      <div class="flex justify-between border-b border-gray-200 pb-2">
        <span class="font-medium text-gray-600">Year:</span>
        <span class="text-gray-800"><%= student.year %></span>
      </div>
      <div class="flex justify-between border-b border-gray-200 pb-2">
        <span class="font-medium text-gray-600">Division:</span>
        <span class="text-gray-800"><%= student.division %></span>
      </div>
      <div class="flex justify-between border-b border-gray-200 pb-2">
        <span class="font-medium text-gray-600">Parent Mobile:</span>
        <span class="text-gray-800"><%= student.parentMobile %></span>
      </div>
      <div class="flex justify-between border-b border-gray-200 pb-2">
        <span class="font-medium text-gray-600">Attendance:</span>
        <span class="text-gray-800"><%= student.attendance %>%</span>
      </div>
    </div>

    <!-- Back Button -->
    <div class="mt-6 text-center">
      <a href="/admin/home"
         class="inline-block px-6 py-2 bg-gradient-to-r from-[#0a9396] to-[#94d2bd] text-white rounded-xl font-semibold hover:from-[#005f73] hover:to-[#0e7c7b] transition duration-300 shadow-md">
        ← Back to Dashboard
      </a>
    </div>
  </div>

</body>
</html>
